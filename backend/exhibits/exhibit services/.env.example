# Exhibit Service environment example
# Copy this file to `.env` and fill any secret values. Do NOT commit your real `.env` to source control.

# Port the service listens on (alternate variable name shown used in code)
PORT=5003
BACKEND_BUILDING_SERVICE_PORT=5003

# PostgreSQL connection settings
DB_USER=postgres
DB_PASSWORD=your_db_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Exhibits

# Notes:
# - Copy this file to `.env` in the same directory as the service before running (or set env vars in your host).
# - For production, use a secrets manager or environment configuration and never commit secrets to git.
